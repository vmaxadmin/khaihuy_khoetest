<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_address_form_inherit" model="ir.ui.view">
        <field name="name">customer_address.res.partner.form.address</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_address_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('o_address_format')]" position="replace">
                <div class="o_address_format">
                    <field name="street" class="o_address_street" placeholder="Street..."/>
                    <field name="street2" placeholder="Street 2... Ward... Distric... City..."/>
                    <field name="ward_id" class="o_address_city" attrs="{'invisible': [('district_id', '=', False)]}"
                           domain="[('district_id', '=', district_id)]" placeholder="Ward"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="district_id" class="o_address_state" attrs="{'invisible': [('city_id', '=', False)]}"
                           domain="[('city_id', '=', city_id)]" placeholder="District"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city_id" class="o_address_zip"
                           domain="[('country_id', '=', country_id)]" placeholder="City"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="country_id" class="o_address_country"
                           domain="[('id', '=', %(base.vn)d)]" placeholder="Country"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city" invisible="1"/>
                    <field name="state_id" invisible="1"/>
                    <field name="zip" invisible="1"/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="res_partner_view_form_private_inherit" model="ir.ui.view">
        <field name="name">customer_address.res.partner.form.private</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.res_partner_view_form_private"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('o_address_format')]" position="replace">
                <div class="o_address_format">
                    <field name="street" class="o_address_street" placeholder="Street..."/>
                    <field name="street2" placeholder="Street 2... Ward... Distric... City..."/>
                    <field name="ward_id" class="o_address_city" attrs="{'invisible': [('district_id', '=', False)]}"
                           domain="[('district_id', '=', district_id)]" placeholder="Ward"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="district_id" class="o_address_state"
                           attrs="{'invisible': [('city_id', '=', False)]}"
                           domain="[('city_id', '=', city_id)]" placeholder="District"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city_id" class="o_address_zip"
                           domain="[('country_id', '=', country_id)]" placeholder="City"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="country_id" class="o_address_country"
                           domain="[('id', '=', %(base.vn)d)]" placeholder="Country"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city" invisible="1"/>
                    <field name="state_id" invisible="1"/>
                    <field name="zip" invisible="1"/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_partner_form_inherit" model="ir.ui.view">
        <field name="name">customer_address.res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('o_address_format')]" position="replace">
                <div class="o_address_format">
                    <field name="street" class="o_address_street" placeholder="Street..."/>
                    <field name="street2" placeholder="Street 2... Ward... Distric... City..."/>
                    <field name="ward_id" class="o_address_city" attrs="{'invisible': [('district_id', '=', False)]}"
                           domain="[('district_id', '=', district_id)]" placeholder="Ward"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="district_id" class="o_address_state" attrs="{'invisible': [('city_id', '=', False)]}"
                           domain="[('city_id', '=', city_id)]" placeholder="District"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city_id" class="o_address_zip"
                           domain="[('country_id', '=', country_id)]" placeholder="City"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="country_id" class="o_address_country"
                           domain="[('id', '=', %(base.vn)d)]" placeholder="Country"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city" invisible="1"/>
                    <field name="state_id" invisible="1"/>
                    <field name="zip" invisible="1"/>
                </div>
            </xpath>

            <xpath expr="//notebook//field[@name='child_ids']/form//div[hasclass('o_address_format')]" position="replace">
                <div class="o_address_format">
                    <field name="street" class="o_address_street" placeholder="Street..."/>
                    <field name="street2" placeholder="Street 2... Ward... Distric... City..."/>
                    <field name="ward_id" class="o_address_city" attrs="{'invisible': [('district_id', '=', False)]}"
                           domain="[('district_id', '=', district_id)]" placeholder="Ward"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="district_id" class="o_address_state" attrs="{'invisible': [('city_id', '=', False)]}"
                           domain="[('city_id', '=', city_id)]" placeholder="District"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city_id" class="o_address_zip"
                           domain="[('country_id', '=', country_id)]" placeholder="City"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="country_id" class="o_address_country"
                           domain="[('id', '=', %(base.vn)d)]" placeholder="Country"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="city" invisible="1"/>
                    <field name="state_id" invisible="1"/>
                    <field name="zip" invisible="1"/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_partner_tree_inherit" model="ir.ui.view">
       <field name="name">customer_address.res.partner.tree</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
          <xpath expr="//field[@name='city']" position="attributes">
            <attribute name="invisible">1</attribute>
          </xpath>
          <xpath expr="//field[@name='state_id']" position="attributes">
            <attribute name="invisible">1</attribute>
          </xpath>
          <xpath expr="//field[@name='city']" position="after">
            <field name="ward_id" optional="show"/>
            <field name="district_id" optional="show"/>
            <field name="city_id" string="City" optional="show"/>
          </xpath>
        </field>
    </record>
</odoo>